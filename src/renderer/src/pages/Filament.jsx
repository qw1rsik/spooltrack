import { Box, FolderKanban, Package, TrendingUp } from 'lucide-react'

function Home() {
  return (
    <div className="flex-1 overflow-auto bg-[var(--bg-color)] p-6 text-[var(--text-color)]">
      <div>
      </div>
    </div>
  )
}

export default Home;